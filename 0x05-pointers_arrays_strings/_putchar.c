#include "main.h"
#include <unistd.h>

/**
 * _putchar - prints a string, in reverse, followed by a new line
 * @s: The string to print
 *
 * Return: void
 */

int _putchar(char v)
{
return (s[c]);
}
